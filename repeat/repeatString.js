const repeatStr = (n, s) => {
    let x = '';
    for( let i = 0; i < n; i++){
      x += s;
    }
    console.log(x);
    return x;
  }

  // criando função que receba dois parametros 1 numero e uma string
  // e repita a string na quantidade de vezs = ao numero